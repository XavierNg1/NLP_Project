 If you have a file ending in dmg you have a disk image. If it ends in zip then you have a compressed file. If it ends in pkg you have a package file. There are other ways of bundling applications but these are the most common.
 The methods below show how to perform each of the tasks depending on the given file.
