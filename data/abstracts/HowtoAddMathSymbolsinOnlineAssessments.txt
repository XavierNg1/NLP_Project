
Use a character map.
A second approach is to create symbols using a XML-based language called MathML (Mathematical Markup Language), which then requires a MathML "plug-in" to be installed to a browser to display the symbol.